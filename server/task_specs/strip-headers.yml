---

name: strip-headers
mode: python

script: "#include<strip-headers.py>"

inputs:
  - name: input_path
    type: string
    target: filepath
    format: text

outputs:
  - name: output_path
    type: string
    target: filepath
    format: text
